---
phase: 01-onboarding-final
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MindRestore/Views/Onboarding/FocusOnboardingPages.swift
autonomous: false
requirements: [ONB-08]
must_haves:
  truths:
    - "Industry Scare page renders the $57B count-up cleanly with mono digits and no jank"
    - "The post-count-up payoff (the surrounding layout, copy, and visual landing) feels intentional, not floating"
    - "Page atmosphere matches OB system (blurs lifted to outer ZStack via `pageAtmosphere` switch if not already)"
    - "Reduce Motion mode shows the final $57B figure immediately with a 0.18s opacity fade"
    - "Voice/copy passes the BRAND.md anti-big-social-media check (no Rx LARP, no clinical chrome)"
  artifacts:
    - path: "MindRestore/Views/Onboarding/FocusOnboardingPages.swift"
      provides: "Polished `FocusOnboardIndustryScare` with isolated weak elements fixed"
      contains: "struct FocusOnboardIndustryScare"
  key_links:
    - from: "FocusOnboardIndustryScare"
      to: "OB design tokens (OB.bg, OB.surface, OB.coral, OB.fg, OB.fg2)"
      via: "Color references"
      pattern: "OB\\.(bg|surface|coral|fg|fg2|fg3|accent)"
---

<objective>
Polish the Industry Scare page (page 4 in onboarding) per CONTEXT D-06. The user said "can be way better tbh." The $57B count-up beat itself is reportedly strong — the weakness is most likely the surrounding layout, the post-count payoff, or the copy/voice. This plan investigates the current implementation on device, isolates the specific weak elements, and applies targeted fixes that match the OB pattern locked in by Pain Cards / Goals / Notif Priming.

Purpose: Industry Scare sits mid-flow between Pain Cards (page 2) and Empathy (page 4 — wait, let me re-check: page list per OnboardingView.swift line 129 has order welcome/name/painCards/industryScare/empathy/goals/age/...) — so Industry Scare comes BEFORE Empathy. Its job is to take the personal pain (Pain Cards) and zoom out to the systemic enemy (the attention-economy industry). If the post-count payoff is weak, the user goes "huh, big number" and moves on without the emotional escalation Empathy needs.

Output: A revised `FocusOnboardIndustryScare` where the weak elements identified during investigation are fixed using OB tokens and the entrance arc cadence from UI-SPEC §"Animation Cadence Pattern".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-onboarding-final/01-CONTEXT.md
@.planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/BRAND.md
@MindRestore/Views/Onboarding/FocusOnboardingPages.swift

<interfaces>
<!-- Existing tokens / components the executor MUST use directly. Do not redeclare. -->

From FocusOnboardingPages.swift (line 12):
```swift
private enum FO {
    // FO-namespaced tokens specific to Focus Mode pages — separate from OB
}
```

From FocusOnboardingPages.swift (line 66):
```swift
struct FocusOnboardIndustryScare: View {
    let onContinue: () -> Void
    // private @State for count-up animation, etc.
}
```

OB design tokens (defined in OnboardingNewScreens.swift line 1312, module-internal):
- `OB.bg` (#0A0A0F), `OB.surface` (#14141F), `OB.border` (white@8%)
- `OB.fg` (white@94%), `OB.fg2` (white@62%), `OB.fg3` (white@40%)
- `OB.accent` (#6890FE), `OB.coral` (#FA6B59), `OB.amber` (#FFC247)
- `OBEyebrow`, `OBContinueButton` are now module-internal (per CONTEXT line 124)

Per UI-SPEC §"Animation Cadence Pattern" — standard arc:
- 0.10s eyebrow + headline fade in (easeOut 0.40s)
- 0.32–0.40s hero (count-up, mascot, etc.) rises (spring 0.50/0.78–0.82)
- 0.78–0.95s mascot lands (light haptic) — if applicable
- 1.20–1.50s subline / caption / privacy line fade in
- 1.45–1.70s CTA + skip fade up

Mono numerals rule (UI-SPEC §"Typography"): ANY hero stat number MUST use `.system(size:weight:design:.monospaced)`. Brand font's proportional digits create visual jitter during count-ups.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Investigate current Industry Scare on device, identify the 2–3 weakest elements</name>
  <read_first>
    - MindRestore/Views/Onboarding/FocusOnboardingPages.swift (entire `FocusOnboardIndustryScare` struct, lines 66 onward — read top-to-bottom; this is investigation, not edit)
    - .planning/phases/01-onboarding-final/01-CONTEXT.md (D-06 — user feedback: "can be way better tbh"; weak element hypothesis: layout/copy/post-count payoff, NOT the count-up itself)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (§"Animation Cadence Pattern" for the entrance arc; §"Typography" for mono numerals; §"Color" for OB token reservations)
    - docs/BRAND.md (voice rules — informed-fear + Gen Z defiance; no Rx LARP; no clinical chrome)
  </read_first>
  <files>(read-only investigation; no edits in this task)</files>
  <action>
Build and install the app on device, advance through onboarding to land on Industry Scare:

```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```

(Skip the build if Plan 01 already produced one this session and `FocusOnboardingPages.swift` is unchanged.)

Read the current `FocusOnboardIndustryScare` source (lines 66 through ~720 — the whole struct). Identify weak spots by comparing to the OB pattern locked in by Welcome / Notif Priming / Pain Cards:

**Inspection checklist (record findings as inline comments in your task summary, do NOT edit yet):**

1. **Eyebrow:** Does it use `OBEyebrow` with brand-font 13pt bold tracking 1.0? Or a custom inline `Text` that drifts from the system?
2. **Headline:** Two-line split-color pattern (`OB.fg` line 1, `OB.accent` or `OB.coral` line 2)? Or a single-color block? Per BRAND voice, the headline should set up the count-up — e.g. `The attention industry is worth\n$57 billion.` with the number on its own dramatic line.
3. **Count-up number:** Is it `.system(size:weight:design:.monospaced)`? Size in the 88–96pt range to dominate the screen? Or `.brand(...)` (which jitters during the count)?
4. **Unit caption (after the number):** Is it mono caps tracking ~1.5pt, OB.fg2? Or some softer body style that competes with the number?
5. **Post-count-up payoff:** What happens AFTER the count settles? Is there a punchline line that lands? A quote? A statistic that personalizes? Or does the page just sit there with a CTA underneath? UI-SPEC §"Page 11 — Plan Reveal" demonstrates the model: the number IS the moment, but a tight follow-up line (e.g. "That's what they spent fighting for your attention") gives the brain somewhere to land.
6. **CTA position + style:** Bottom-anchored `OBContinueButton`? Or a custom button that drifts from the OB style?
7. **Atmosphere:** Does the page background carry an OB.coral or OB.accent atmosphere blur (warm-side cool-side mix)? Or is it flat OB.bg with nothing behind the number? The Welcome bouncer has dual-blur atmosphere — Industry Scare can carry the same to feel more cinematic.
8. **Voice/copy check (BRAND.md):**
   - Avoid "Rx", "Prescription", "Protocol", clinical-LARP words (forbidden globally per UI-SPEC anti-pattern guards).
   - Lean into the two-enemies doctrine: name the industry as the enemy, not the user's brain.
   - Defiance, not despair: the count-up should escalate the user's resolve, not bury them.
9. **Reduce Motion:** Does the page have an `accessibilityReduceMotion` fallback that shows the final $57B immediately with a 0.18s opacity fade?
10. **Animation cadence:** Does the entrance follow the standard arc (eyebrow/headline @ 0.10s → number rises @ 0.32s → count-up runs over ~1.5–1.8s → unit caption + payoff line @ end of count → CTA @ end+0.30s)?

Pick **the 2 to 3 highest-impact weaknesses** and write them down as the targets for Task 2. Do not over-scope — this is polish, not redesign. If the page is already strong on most dimensions, the deliverable might be only 1 or 2 small fixes.

If on-device inspection reveals the page is already at OB polish (rare but possible — the user's "can be way better" might have been a soft signal), document that and reduce Task 2 to a no-op + verification.
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Investigation summary written into the executor's response (not a file): lists current strengths + 2–3 specific weak elements with line-number references
    - Each weak element is mapped to a specific UI-SPEC section (e.g., "Eyebrow at line 84 uses inline Text — should adopt `OBEyebrow` per UI-SPEC §Component Inventory")
    - Build still succeeds (no edits made yet, but baseline build confirms current state compiles)
  </acceptance_criteria>
  <done>
    - Weak-element list with line numbers + UI-SPEC mappings ready for Task 2
    - Device baseline build verified
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply targeted fixes to the 2–3 weakest elements identified in Task 1</name>
  <read_first>
    - The investigation notes from Task 1 (in-context)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (cite the specific section + values for each fix)
    - MindRestore/Views/Onboarding/FocusOnboardingPages.swift
    - MindRestore/Views/Onboarding/OnboardingNewScreens.swift (lines 1312–1351 — to confirm OB tokens + OBEyebrow + OBContinueButton are accessible from this file; they should be since both files are in the same target/module)
  </read_first>
  <files>MindRestore/Views/Onboarding/FocusOnboardingPages.swift</files>
  <action>
Apply ONLY the fixes identified in Task 1. Do not rewrite the page. For each fix, follow the verbatim UI-SPEC values from the relevant section.

**Common patterns the executor will likely apply (use as templates if applicable):**

**If eyebrow drifts from system:**
- Replace inline `Text("...").font(...).tracking(...).foregroundStyle(...)` with `OBEyebrow(text: "...")` — use the existing component.

**If headline isn't split-color:**
- Build with `Text("...") + Text("...")` concat where line 2 uses `.foregroundStyle(OB.accent)` or `.foregroundStyle(OB.coral)` per voice intent (coral for "industry as enemy" framing, accent for "we have a plan" framing).
- Size: `.brand(size: 38, weight: .heavy)`, lineSpacing 2–4.

**If the count-up number isn't mono:**
- Replace `.brand(size: 88, weight: .heavy)` with `.system(size: 92, weight: .heavy, design: .monospaced)` — this is the same hero-stat treatment locked in for Plan Reveal Layer 1+2 (UI-SPEC §"Page 11 — Plan Reveal" visual spec table).
- Foreground: `OB.fg` for the white "shock" number, OR `OB.coral` if the industry framing wants the warning red. Pick one and stay consistent.

**If post-count payoff is missing or weak:**
- Add a one-line follow-up under the unit caption with `.brand(size: 15, weight: .semibold)` and `OB.fg2`.
- Copy idea (executor's discretion within BRAND voice): a single sentence that names the enemy and pivots toward Memo. Example: `That's what they spent fighting for your attention. Memo's gonna fight back.` (BRAND.md "fight back" + two-enemies doctrine.)
- Animate this line in at `t = end_of_count + 0.30s`, easeOut 0.40s, opacity 0→1, y 6→0.

**If atmosphere blur is missing:**
- Add a case to `pageAtmosphere` in `OnboardingView.swift`'s `pageAtmosphere` switch IF AND ONLY IF the on-device baseline shows the page is too flat. Add `OB.coral.opacity(0.10)` blur 68pt on warm side. Note: this edits `OnboardingView.swift` — check if Plan 03 (Goals) is touching the same switch in the same wave; if so, sequence carefully or coordinate.

**Reduce Motion fallback:**
- Add `@Environment(\.accessibilityReduceMotion) private var reduceMotion`.
- When `reduceMotion == true`, skip the count-up: render the final $57B value immediately with a 0.18s opacity fade. Skip the spring rises; use 0.18s opacity fades for headline, count, unit, payoff, CTA. Haptics preserved (light haptic on count settle becomes light haptic on opacity-fade complete).

**Forbidden (UI-SPEC anti-pattern guards):**
- No "Rx", "Prescription", "Protocol", "Memori Protocol" copy.
- No clinical chrome (signature lines, version numbers).
- No SF-Symbol icon tile constellations replacing real assets.
- No checkered placeholder behind any mascot.

**Build & verify:**
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -10
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```

Run the `/verify-changes` skill per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'design: \\.monospaced' MindRestore/Views/Onboarding/FocusOnboardingPages.swift` finds the count-up number using mono design (if Task 1 flagged it as non-mono)
    - `grep -n "accessibilityReduceMotion" MindRestore/Views/Onboarding/FocusOnboardingPages.swift` finds the Reduce Motion environment binding
    - `grep -nE '"Rx"|"Prescription"|"Protocol"' MindRestore/Views/Onboarding/FocusOnboardingPages.swift` returns NO matches
    - `xcodebuild` returns `BUILD SUCCEEDED`
    - On-device manual: count-up still hits cleanly; the 2–3 fixes flagged in Task 1 are visibly addressed; voice still reads as Memo (not clinical)
  </acceptance_criteria>
  <done>
    - Targeted fixes applied (no rewrite); each fix maps to a Task 1 finding
    - Build + install succeed on device
    - User has visually verified the polished Industry Scare page on iPhone
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Industry Scare on iPhone</name>
  <what-built>The 2–3 highest-impact polish fixes from Task 1, applied per UI-SPEC values. Reduce Motion fallback added. No rewrite — surgical polish.</what-built>
  <how-to-verify>
On iPhone, advance through onboarding to land on Industry Scare:
1. Verify the entrance arc reads cleanly: eyebrow → headline → count-up rises → number runs 0 → $57B over ~1.5–1.8s → unit caption → payoff line → CTA. No jank, no jitter on digits.
2. Verify the count-up uses MONO digits (no proportional shifting during the count).
3. Verify the post-count payoff line lands with intent — there's a sentence under the number that gives the brain somewhere to go.
4. Verify the voice still reads Memo (defiant, gen-Z, anti-attention-industry) — NOT clinical, NOT prescriptive.
5. Toggle Reduce Motion ON. Verify final $57B shows immediately with a clean 0.18s fade and no count-up.
6. Verify the page renders dark in iOS light-mode setting too (`.preferredColorScheme(.dark)` pinned).
7. Compare side-by-side with Welcome bouncer + Pain Cards (now polished) — does Industry Scare feel like it belongs in the same family?
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still feels off — Claude will iterate (max 1 more pass within this plan; deeper rework would be a separate plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Industry Scare is a static informational page. No user input, no permission prompts, no network calls, no persistent storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | $57B figure | accept | Public industry-economy figure; cited in BRAND.md as the kind of defensible stat used in onboarding voice. No claim of attribution to a specific source needed for low-stakes UX copy. |

**Severity: low/none — UI polish only, no permission changes, no network, no persistent storage of sensitive data.**
</threat_model>

<verification>
- `xcodebuild` succeeds.
- Each Task 1 finding has a corresponding code change in Task 2.
- Reduce Motion fallback present.
- Voice/copy passes BRAND.md check (no clinical chrome, two-enemies doctrine intact).
</verification>

<success_criteria>
- 2–3 weak elements isolated and fixed (or page declared already-strong with documented justification)
- Device build + install succeed
- Human-verified on iPhone
- Page now feels like it belongs alongside Welcome / Pain Cards / Notif Priming
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-final/01-02-SUMMARY.md` per `templates/summary.md`. Include the Task 1 weak-element findings + Task 2 fix list + Task 3 verification result.
</output>
