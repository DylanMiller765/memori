---
phase: 01-onboarding-final
plan: 07
type: execute
wave: 4
depends_on: [06]
files_modified:
  - MindRestore/Views/Onboarding/OnboardingView.swift
autonomous: false
requirements: [ONB-06]
must_haves:
  truths:
    - "Empathy page (page 4 in current order: welcome/name/painCards/industryScare/empathy/goals) has been verified live on device against UI-SPEC §'Page 6 — Empathy interstitial'"
    - "If verification reveals defects, targeted fixes are applied to match UI-SPEC §'Page 6'"
    - "Page dots are HIDDEN on Empathy"
    - "Headline split-color works (line 1 OB.fg, line 2 OB.accent)"
    - "Mascot renders on the dark page directly with NO white container, NO checkered transparency placeholder"
    - "Subline reads exactly: '47 apps are fighting for your attention every minute. Memo's gonna fight back with you.'"
    - "CTA reads exactly: 'Show me my plan'"
    - "Reduce Motion mode shows opacity-only transitions; haptics on line-2 land preserved"
  artifacts:
    - path: "MindRestore/Views/Onboarding/OnboardingView.swift"
      provides: "Verified-and-polished `empathyPage` private computed view"
      contains: "private var empathyPage"
  key_links:
    - from: "Empathy page mascot"
      to: "Asset catalog `mascot-goal` (or chosen pose per UI-SPEC §'Mascot Pose Catalog')"
      via: "Image(\"mascot-goal\") rendered on dark page directly"
      pattern: "Image\\(\"mascot"
---

<objective>
Verify-and-polish the Empathy interstitial (page 4 in current order, page 6 in the documented 16-page sequence) per CONTEXT D-07. The user said "current state on device is uncertain — walk through it on iPhone during Phase 1 implementation and decide live whether it needs polish or is fine. If polish needed, apply within this phase. If it's already strong, skip."

This plan is intentionally smaller than the others: a verify pass, then conditional fixes if needed. UI-SPEC §"Page 6 — Empathy interstitial" provides the locked spec to verify against.

Per the existing on-disk read (line 861–943 of `OnboardingView.swift`), the Empathy page already has:
- A `EmpathySignalBackdrop`, `EmpathyFeedWallScene` (Codex's Option 3 — "sunglasses Memo against social media wall" — appears partially built)
- `Text("isn't broken.")` and `Text("It's been hijacked.")` headline split
- Animation states (`empathySceneVisible`, `empathySignalBroken`, `empathyCopyVisible`, `empathyCTAVisible`)
- `resetEmpathyAnimation()` cleanup

The job: confirm what's working, identify what's not, apply targeted fixes only if defects are visible on device.

Purpose: Empathy is the emotional pivot — pain (Pain Cards) zooms out to industry (Industry Scare) zooms back in to brain (Empathy: "your brain isn't broken, it's been hijacked"). It HAS to land. If it's already landing, leave it. If it's broken (mascot missing, page dots leaking, copy off, light-mode bleed), fix it surgically.

Output: Either a "no-op verified" report OR a polished `empathyPage` matching UI-SPEC §"Page 6" acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-onboarding-final/01-CONTEXT.md
@.planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/BRAND.md
@MindRestore/Views/Onboarding/OnboardingView.swift

<interfaces>
<!-- Existing tokens / structs / locations the executor MUST use. -->

OB design tokens:
- `OB.bg`, `OB.surface`, `OB.fg`, `OB.fg2`, `OB.accent`

Empathy page location:
- `MindRestore/Views/Onboarding/OnboardingView.swift:863` — `private var empathyPage: some View`
- Supporting `EmpathySignalBackdrop` and `EmpathyFeedWallScene` private structs nearby

Mascot pose catalog (UI-SPEC §"Mascot Pose Catalog"):
- `mascot-goal` — Determined, ready-to-fight, eyes forward — RECOMMENDED for Empathy Option 3

UI-SPEC §"Page 6 — Empathy interstitial" copywriting contract:
- Eyebrow: NONE (full-bleed editorial moment, no eyebrow)
- Headline line 1: `Your brain isn't broken.` (`OB.fg`)
- Headline line 2: `It's been hijacked.` (`OB.accent`)
- Subline: `47 apps are fighting for your attention every minute. Memo's gonna fight back with you.`
- CTA: `Show me my plan`
- Page dots: HIDDEN

UI-SPEC §"Page 6" visual spec table:
- Background: `OB.bg` with subtle atmosphere blur — `OB.accent.opacity(0.16)` circle 320pt, blur 90, offset (0, -200) behind mascot
- Mascot: `mascot-goal` rendered at 220pt height, centered horizontally, vertical position 35–40% from page top
- Headline line 1: `.brand(size: 38, weight: .heavy)`, `OB.fg`, `lineSpacing 4`
- Headline line 2: `.brand(size: 38, weight: .heavy)`, `OB.accent` (concat with line 1 via `Text + Text`)
- Subline: `.brand(size: 15, weight: .semibold)`, `OB.fg2`, 28pt horizontal margin, multiline, max 4 lines
- CTA: `OBContinueButton`, title `Show me my plan`

Anti-pattern guards:
- Mascot is on the dark page directly. NO white container. NO checkered transparency placeholder.
- NO icon-tile pattern of any kind.
- NO eyebrow on this page.

Animation timeline:
- t=0.20s: Mascot fades up (opacity 0→1, scale 0.92→1.0, 0.55s spring response 0.50, damping 0.78)
- t=0.50s: Headline line 1 fades up (opacity 0→1, y 8→0, 0.40s easeOut)
- t=0.65s: Headline line 2 fades up + LIGHT HAPTIC on color land
- t=1.00s: Subline fades up (opacity 0→1, y 6→0, 0.35s easeOut)
- t=1.35s: CTA fades up (0.40s easeOut)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify Empathy on device against UI-SPEC §"Page 6" acceptance criteria</name>
  <read_first>
    - MindRestore/Views/Onboarding/OnboardingView.swift (lines 38–45 for empathy state declarations + lines 863–960 for `empathyPage` + supporting structs)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (§"Page 6 — Empathy interstitial" lines 564–629)
    - .planning/phases/01-onboarding-final/01-CONTEXT.md (D-07 — verify on device, decide live)
    - MindRestore/Assets.xcassets/ (look for `mascot-goal` to confirm asset exists; if missing, the impl may be using a different pose — note for Task 2)
  </read_first>
  <files>(read-only verification; no edits unless Task 2 fires)</files>
  <action>
Build + install if not fresh:
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```

**Walk through onboarding to Empathy. Test against UI-SPEC §"Page 6" acceptance criteria (lines 620–628):**

1. **Page dots HIDDEN?** Verify no progress dots visible on Empathy. (Per UI-SPEC line 597.)
2. **Mascot on dark page directly?** Verify the mascot renders without a white container, without a checkered transparency placeholder.
3. **Headline split-color works?** Line 1 white, line 2 OB.accent (#6890FE).
4. **Subline copy verbatim?** Exactly: `47 apps are fighting for your attention every minute. Memo's gonna fight back with you.`
5. **CTA copy verbatim?** Exactly: `Show me my plan`.
6. **Reduce Motion mode shows opacity-only transitions?** Toggle Reduce Motion in iOS Settings → Accessibility → Motion. Re-launch. Verify entrance is fade-only, no scale or spring.
7. **Build succeeds on device?** (Always — baseline.)

**Beyond acceptance criteria, also check:**
8. **Atmosphere blur present?** UI-SPEC says `OB.accent.opacity(0.16)` 320pt circle blur 90pt offset (0, -200). If the page feels too flat or too noisy, the atmosphere might be off.
9. **Voice/copy passes BRAND.md check?** "Memo's gonna fight back with you" reads as Gen-Z defiance — preserved.
10. **Light-mode parity?** With iOS in light mode, does Empathy still render dark? (`.preferredColorScheme(.dark)` pinned on the page or the parent OnboardingView.)
11. **Existing Codex impl elements (`EmpathySignalBackdrop`, `EmpathyFeedWallScene`) — are they working?** This was Codex's Option 3 ("sunglasses Memo against social media wall"). If the social-media wall scene renders cleanly with logos behind a sunglasses Memo, that's a richer execution than UI-SPEC's minimum (which calls for just `mascot-goal` on dark bg). Per CONTEXT D-07, EITHER variant is acceptable as long as anti-pattern guards hold (no white container, no checkered placeholder, no eyebrow, no icon-tile constellation).

**Output:**
Document explicitly:
- (a) Each of the 7 UI-SPEC acceptance criteria: PASS / FAIL.
- (b) For each FAIL: line number(s) of the offending code + the UI-SPEC value to fix to.
- (c) Whether Codex's Option 3 (`EmpathySignalBackdrop` + `EmpathyFeedWallScene`) is rendering or broken.
- (d) Verdict: **NO-OP** (page is verified-strong, skip Task 2) OR **POLISH** (Task 2 fires with specific fixes listed).
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Verification report includes all 7 UI-SPEC §"Page 6" acceptance-criteria results (each PASS or FAIL with evidence)
    - Verdict written: NO-OP or POLISH (with fix list)
    - Build succeeds (baseline)
  </acceptance_criteria>
  <done>
    - Verification report complete; downstream task either fires (POLISH) or is skipped (NO-OP)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (CONDITIONAL — only if Task 1 verdict is POLISH): Apply targeted fixes to match UI-SPEC §"Page 6"</name>
  <read_first>
    - Task 1 verification report
    - MindRestore/Views/Onboarding/OnboardingView.swift (relevant Empathy region)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (§"Page 6 — Empathy interstitial" — pull specific values per fix)
  </read_first>
  <files>MindRestore/Views/Onboarding/OnboardingView.swift</files>
  <action>
**SKIP THIS TASK if Task 1 verdict is NO-OP.** Document NO-OP in the SUMMARY and proceed to Task 3.

If POLISH, apply ONLY the fixes flagged in Task 1's fix list. Do NOT rewrite the page.

**Common fixes (apply each ONLY if flagged):**

**Fix — Page dots not hidden:**
- Locate the parent's page-dots renderer (likely a switch on `currentPage` in `OnboardingView.swift`'s body). Confirm Empathy's page index is in the hide-list. Per UI-SPEC line 805, pages 6 (Empathy) and 11 (Plan Reveal) hide page dots. If Empathy's index in the current order is `4`, that's the value to gate on. If Plan 04 (Plan Reveal) flagged that page-dots hiding needed extending into OnboardingView, this is the right plan to add it (since Empathy is also dot-hidden and we're already in this file).
- Pattern (verify against actual page-dots impl):
  ```swift
  if currentPage != 4 && currentPage != 10 {  // hide on empathy + planReveal
      progressDotsView
  }
  ```

**Fix — Headline copy drift:**
- Confirm `Text("Your brain")` + `Text("\nisn't broken.")` followed by `Text("It's been hijacked.")` matches verbatim. Adjust if any character drift.

**Fix — Subline copy drift:**
- Verbatim: `47 apps are fighting for your attention every minute. Memo's gonna fight back with you.`
- Replace any near-miss copy with this exact string.

**Fix — CTA copy drift:**
- Verbatim: `Show me my plan`. Adjust if needed.

**Fix — Mascot not rendering, or rendering with white container:**
- Search for `Image("mascot-goal")` (or whichever pose). Verify the parent has NO `.background(.white)` or `.background(Color.white)`.
- Verify the `Image` modifiers: `.resizable().aspectRatio(contentMode: .fit).frame(height: 220)`.
- Center horizontally: wrap in `HStack { Spacer(); mascot; Spacer() }` or use `.frame(maxWidth: .infinity, alignment: .center)`.
- Apply accent glow per UI-SPEC: `.shadow(color: OB.accent.opacity(0.32), radius: 28, x: 0, y: 12)` if absent.

**Fix — Atmosphere blur missing or wrong:**
- Add to `pageAtmosphere` switch (line 181 of OnboardingView.swift) for Empathy's page index:
  ```swift
  case 4:  // Empathy
      OB.accent.opacity(0.16)
          .frame(width: 320, height: 320)
          .blur(radius: 90)
          .offset(x: 0, y: -200)
  ```
- (Adjust the case value to match the actual `currentPage` index for Empathy.)

**Fix — Reduce Motion fallback missing:**
- Add `@Environment(\.accessibilityReduceMotion) private var reduceMotion` if absent.
- Wrap entrance animations: when `reduceMotion == true`, replace springs/scales with 0.18s opacity fades. Preserve the light haptic on line-2 land.

**Fix — Light-mode bleed:**
- Verify `.preferredColorScheme(.dark)` is pinned (likely on the parent `OnboardingView`'s body, not per-page). If a sub-view introduces a light background (e.g. `EmpathyFeedWallScene` using `.white` accidentally), patch with OB tokens.

**Fix — Eyebrow present (forbidden):**
- UI-SPEC line 597: "NO eyebrow on this page." If an `OBEyebrow(...)` exists in `empathyPage`, REMOVE it.

**Fix — Icon-tile / SF-Symbol constellation:**
- UI-SPEC anti-pattern: NO icon-tile pattern. If `EmpathyFeedWallScene` is rendering a grid of SF Symbols instead of real social logo assets (`logo-tiktok`, `logo-instagram`, etc.), patch to use the real PNGs from `Assets.xcassets/`.

**Build & verify:**
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -10
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Each Task 1 FAIL is now a PASS on re-verification
    - `grep -nE '"Show me my plan"' MindRestore/Views/Onboarding/OnboardingView.swift` finds the verbatim CTA
    - `grep -n "47 apps are fighting" MindRestore/Views/Onboarding/OnboardingView.swift` finds the verbatim subline
    - `grep -n "isn't broken" MindRestore/Views/Onboarding/OnboardingView.swift` finds the verbatim headline line 1 fragment
    - `grep -n "It's been hijacked" MindRestore/Views/Onboarding/OnboardingView.swift` finds the verbatim headline line 2
    - No `.background(Color.white)` or `.background(.white)` inside `empathyPage` body or supporting structs
    - `xcodebuild` returns `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>
    - All Task 1 FAILs converted to PASS
    - OR no edits made (Task 1 verdict was NO-OP)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Empathy on iPhone (final)</name>
  <what-built>Empathy verified against UI-SPEC §"Page 6" acceptance criteria. Targeted fixes applied if needed (or no-op if already strong).</what-built>
  <how-to-verify>
On iPhone, advance to Empathy. Confirm all 7 UI-SPEC §"Page 6" acceptance criteria PASS:
1. Page dots HIDDEN.
2. Mascot on dark page (NO white container, NO checkered placeholder).
3. Split-color headline: line 1 white, line 2 OB.accent.
4. Subline reads exactly: `47 apps are fighting for your attention every minute. Memo's gonna fight back with you.`
5. CTA reads exactly: `Show me my plan`.
6. Reduce Motion ON → opacity-only transitions, light haptic on line-2 land preserved.
7. Build succeeds on device.

Bonus (D-07 verification):
8. The page reads as a peak emotional moment, not a generic interstitial.
9. Light-mode iOS setting → Empathy stays dark.
  </how-to-verify>
  <resume-signal>Type "approved" or list remaining defects.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Empathy is a static editorial page. No user input, no permission prompts, no network, no persistent storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | "47 apps" stat | accept | Public industry stat used in BRAND voice. Low stakes. |

**Severity: low/none — UI polish only, no permission changes, no network, no persistent storage of sensitive data.**
</threat_model>

<verification>
- `xcodebuild` succeeds.
- All 7 UI-SPEC §"Page 6" acceptance criteria PASS on device.
- Reduce Motion fallback works; haptic on line-2 land preserved.
- No light-mode leak.
</verification>

<success_criteria>
- Empathy verified strong (NO-OP) OR polished to UI-SPEC §"Page 6" (POLISH)
- Build + install succeed
- Human-verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-final/01-07-SUMMARY.md` per `templates/summary.md`. Document the Task 1 verdict (NO-OP or POLISH) and the fix list applied (if any).
</output>
