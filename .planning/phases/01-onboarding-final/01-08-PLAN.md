---
phase: 01-onboarding-final
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - MindRestore/Views/Onboarding/OnboardingView.swift
  - MindRestore/Views/Onboarding/OnboardingNewScreens.swift
  - MindRestore/Views/Onboarding/FocusOnboardingPages.swift
autonomous: false
requirements: [ONB-01, ONB-02, ONB-03, ONB-08, ONB-09]
must_haves:
  truths:
    - "ONB-01 (Welcome bouncer), ONB-02 (Notif Priming counter-cards), ONB-03 (atmosphere lift) are confirmed shipped and working on the current build (verification only — no code changes)"
    - "ONB-09 receipt-count plumbing is confirmed end-to-end on a fresh device run (Pain Cards N>0 → Plan Reveal copy variant A; N=0 → Plan Reveal copy variant B)"
    - "All 16 onboarding pages render correctly on iPhone in BOTH iOS light-mode and iOS dark-mode system settings (`.preferredColorScheme(.dark)` pinned across the flow)"
    - "Status bar reads light content on every page; modals/sheets inherit dark"
    - "iPhone SE 3 (375pt frame) renders without truncation/overflow on every page; iPhone 15 Pro Max (430pt) renders without ungainly stretching"
    - "VoiceOver reads page content in logical order on every page"
    - "Reduce Motion produces fade-only transitions on the 6 polished pages (Pain Cards, Goals, Industry Scare, Plan Reveal, Commitment, Screen Time, Empathy if polished)"
    - "On-device frame rate at 60fps during entrance animations on every page (no jank during count-up)"
    - "No `Memori` user-facing string drift visible (BRAND-05 is Phase 3 scope, but polish work introduces no new `Memori` references — flag only)"
  artifacts:
    - path: "(verification artifact only — no new files)"
      provides: "ONB-08 audit checklist completed for all 16 pages"
      contains: "(documented in 01-08-SUMMARY.md)"
  key_links:
    - from: "ONB-09 receipt-count plumbing"
      to: "End-to-end Pain Cards → OnboardingView state → Plan Reveal copy"
      via: "Verified live on device with N=4 and N=0 runs"
      pattern: "receiptCount"
---

<objective>
Final phase sweep: light/dark parity audit across all 16 onboarding pages (ONB-08), end-to-end verification of receipt-count plumbing (ONB-09), and explicit acknowledgment that ONB-01/02/03 (already shipped) are confirmed working on the current build.

Per CONTEXT D-12: "After all 6 polish targets are complete, walk all 16 pages in light + dark mode on device, capture any rendering defects, fix in the same phase commit batch."

Per CONTEXT D-11: "Apply [Reduce Motion fallbacks] to existing strong pages opportunistically only if a defect is observed on device — not a sweep on already-shipped pages."

This is the final wave — all polish targets (Plans 01–07) have completed by the time this plan runs.

Purpose: Single-pass sweep catches the integration-level defects that single-page work misses. Examples: a status-bar style leak on one page bleeds into the next; a modal sheet inherits light mode by default; a TextField on the Name page shows a light keyboard. The sweep is fast (one device walk) but high-leverage.

Output: Audit report covering all 16 pages × the ONB-08 checklist (UI-SPEC lines 825–838). Targeted fixes for any defects observed. Acknowledgment that ONB-01/02/03 are confirmed shipped. End-to-end verification of ONB-09 plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-onboarding-final/01-CONTEXT.md
@.planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md
@.planning/phases/01-onboarding-final/01-01-SUMMARY.md
@.planning/phases/01-onboarding-final/01-02-SUMMARY.md
@.planning/phases/01-onboarding-final/01-03-SUMMARY.md
@.planning/phases/01-onboarding-final/01-04-SUMMARY.md
@.planning/phases/01-onboarding-final/01-05-SUMMARY.md
@.planning/phases/01-onboarding-final/01-06-SUMMARY.md
@.planning/phases/01-onboarding-final/01-07-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/BRAND.md

<interfaces>
<!-- Existing already-shipped page pointers — verify, do not modify. -->

ONB-01 Welcome (page 0):
- `MindRestore/Views/Onboarding/OnboardingView.swift` — `welcomePage` + `WelcomeBouncerHero` + `WelcomeAppLogo`
- Spec: `docs/superpowers/specs/2026-04-27-welcome-redesign-design.md`
- Shipped commit: `c5a73ac` (per STATE.md)

ONB-02 Notif Priming (page 14):
- `MindRestore/Views/Onboarding/OnboardingNewScreens.swift` — `OBEyebrow(text: "TWO KINDS OF NUDGES")` at line 679
- Spec: `docs/superpowers/specs/2026-04-27-notif-priming-redesign-design.md`
- Shipped commit: `c5a73ac`

ONB-03 Atmosphere lift (architectural):
- `MindRestore/Views/Onboarding/OnboardingView.swift:181` — `pageAtmosphere` switch on outer ZStack
- Shipped commit: `c5a73ac`

UI-SPEC §"ONB-08 — Light/dark parity audit checklist" (lines 823–838):
For each page (Welcome, Name, Pain Cards, Industry Scare, Age, Goals, Empathy, Screen Time, Personal Scare, Quick Assessment, Plan Reveal, Comparison, Differentiation, Focus Mode setup, Notif Priming, Commitment):
- [ ] `.preferredColorScheme(.dark)` is pinned at the page root
- [ ] Status bar reads light content
- [ ] Modal sheets / popovers (if any) inherit dark
- [ ] Keyboard appearance is dark on TextField pages (Name)
- [ ] No hardcoded `Color.white` / `Color.black` / `.background(.white)` leaks
- [ ] All color references use `OB.*` tokens (not `AppColors.*`)
- [ ] iPhone SE 3 (375pt) renders without truncation / overflow
- [ ] iPhone 15 Pro Max (430pt) renders without ungainly stretching
- [ ] VoiceOver reads page content in logical order
- [ ] Reduce Motion produces fade-only transitions
- [ ] Build succeeds on physical device target `00008130-000A214E11E2001C`
- [ ] On-device frame rate at 60fps during entrance (no jank during count-up)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Acknowledge ONB-01/02/03 already shipped + grep for `.preferredColorScheme(.dark)` and color-leak patterns across the 3 onboarding source files</name>
  <read_first>
    - .planning/phases/01-onboarding-final/01-CONTEXT.md (already-shipped list — ONB-01/02/03 are mark-complete-on-entry)
    - MindRestore/Views/Onboarding/OnboardingView.swift (specifically the body of the page-host TabView/ScrollView wrapper, find `.preferredColorScheme(.dark)`)
    - MindRestore/Views/Onboarding/OnboardingNewScreens.swift
    - MindRestore/Views/Onboarding/FocusOnboardingPages.swift
  </read_first>
  <files>(read-only audit; no edits unless Task 2 fires)</files>
  <action>
**Step 1 — Acknowledge already-shipped:**

Per CONTEXT line 33–35 and ROADMAP "Already shipped" section:
- ONB-01: Welcome bouncer scene + struggle-and-shove animation — shipped 2026-04-27 (commit `c5a73ac`)
- ONB-02: Notif Priming counter-cards (TikTok feed-bait vs Memo unlock) — shipped 2026-04-27 (commit `c5a73ac`)
- ONB-03: Atmosphere lift (blurs to outer ZStack, behind progress bar) — shipped 2026-04-27 (commit `c5a73ac`)

These three requirements are NOT being implemented in this plan — they're verified as part of the device walkthrough in Task 3 below. Note in the SUMMARY that they pass.

**Step 2 — Static grep audit:**

Run the following greps from the repo root and record the results:

```bash
# (a) Confirm `.preferredColorScheme(.dark)` is pinned at the parent or per-page
grep -nE '\\.preferredColorScheme\\(\\.dark\\)' MindRestore/Views/Onboarding/OnboardingView.swift MindRestore/Views/Onboarding/OnboardingNewScreens.swift MindRestore/Views/Onboarding/FocusOnboardingPages.swift

# (b) Find raw white/black hardcoded leaks
grep -nE '\\.background\\(\\.white\\)|\\.background\\(Color\\.white\\)|\\.foregroundStyle\\(Color\\.black\\)|\\.foregroundStyle\\(\\.black\\)|\\.foregroundColor\\(Color\\.white\\)|\\.foregroundColor\\(\\.white\\)' MindRestore/Views/Onboarding/OnboardingView.swift MindRestore/Views/Onboarding/OnboardingNewScreens.swift MindRestore/Views/Onboarding/FocusOnboardingPages.swift

# (c) Find AppColors.* leaks (shouldn't be in onboarding — should use OB tokens only)
grep -nE 'AppColors\\.' MindRestore/Views/Onboarding/OnboardingView.swift MindRestore/Views/Onboarding/OnboardingNewScreens.swift MindRestore/Views/Onboarding/FocusOnboardingPages.swift

# (d) Find any user-facing "Memori" string (case-sensitive, distinguishing from "MindRestore" the codebase folder)
# Phase 3 (BRAND-05) handles the rename, but flag any new ones introduced by polish work
grep -nE '"Memori"|>Memori<|\\bMemori\\b' MindRestore/Views/Onboarding/OnboardingView.swift MindRestore/Views/Onboarding/OnboardingNewScreens.swift MindRestore/Views/Onboarding/FocusOnboardingPages.swift

# (e) Find Color.white / Color.black raw refs
grep -nE 'Color\\.white|Color\\.black' MindRestore/Views/Onboarding/OnboardingView.swift MindRestore/Views/Onboarding/OnboardingNewScreens.swift MindRestore/Views/Onboarding/FocusOnboardingPages.swift
```

Note: `Color.white.opacity(...)` is a LEGITIMATE pattern used in OB tokens (e.g., `OB.border = Color.white.opacity(0.08)`). These are NOT defects — they're the documented opacity-on-white technique for subtle borders/dividers on dark surfaces. Distinguish from raw `Color.white` used as full fills.

**Step 3 — Document findings:**

For each grep result, classify:
- **Expected** (e.g., `Color.white.opacity(0.08)` for OB.border definitions)
- **Defect** (e.g., `.background(Color.white)` on a card surface)
- **Flag for Phase 3** (e.g., user-facing "Memori" string — Phase 3 BRAND-05 handles rename)

Output a categorized list. Defect items become Task 2's fix targets.
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Grep audit report produced with all 5 grep commands' output classified
    - ONB-01/02/03 explicitly acknowledged as shipped (commit `c5a73ac`)
    - Any defect items listed for Task 2
    - Build succeeds (baseline)
  </acceptance_criteria>
  <done>
    - Static audit complete; defect list produced
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply fixes for any defects found in Task 1's static grep audit</name>
  <read_first>
    - Task 1 audit report
    - The specific files flagged with defects
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (§"Color" for OB token reservations)
  </read_first>
  <files>MindRestore/Views/Onboarding/OnboardingView.swift, MindRestore/Views/Onboarding/OnboardingNewScreens.swift, MindRestore/Views/Onboarding/FocusOnboardingPages.swift</files>
  <action>
**SKIP THIS TASK if Task 1 found NO defects.**

For each defect:
- **Raw `Color.white` / `.background(.white)` leak:** Replace with the appropriate OB token (e.g., `OB.surface` for a card fill, `OB.border` for a thin border).
- **`AppColors.*` leak:** Replace with the equivalent OB token. Per UI-SPEC §"Color" line 92: "do NOT use `AppColors.*` (those are for the post-onboarding app surface)."
- **Missing `.preferredColorScheme(.dark)`:** Verify it's pinned on the parent `OnboardingView` body. If not, add it once at the top-level VStack or ZStack of `OnboardingView.body`. Do NOT pin per-page (one pin at the parent suffices).

**Do NOT fix `Memori` strings in this plan** — that's Phase 3 BRAND-05 scope. Just document them in the SUMMARY for handoff.

**Build & verify:**
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -10
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 defect items resolved (or NO-OP if none)
    - Re-run Task 1 grep commands; defect items now return NO matches (or are explicitly Phase-3-deferred)
    - `xcodebuild` returns `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>
    - Defects fixed (or no-op if none)
    - Build + install succeed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human device walkthrough — all 16 pages × ONB-08 checklist + ONB-09 end-to-end + ONB-01/02/03 verification</name>
  <what-built>Final phase sweep on iPhone. ONB-01/02/03 verified shipped + working. ONB-08 light/dark parity audit run on all 16 pages. ONB-09 receipt-count plumbing verified end-to-end on a fresh device run.</what-built>
  <how-to-verify>
On iPhone (`00008130-000A214E11E2001C`), do **two complete onboarding walkthroughs** plus a Reduce Motion pass:

**Walk A — N=4 receipt run, dark mode (iOS Settings → Display & Brightness → Dark):**

Fresh-install. Walk all 16 pages in order: Welcome → Name → Pain Cards → Industry Scare → Empathy → Goals → Age → Screen Time Access → Personal Scare → Quick Assessment → Plan Reveal → Comparison → Differentiation → Focus Mode setup → Notif Priming → Commitment.

For each page, verify the ONB-08 checklist (UI-SPEC lines 825–838):
- [ ] Background is `OB.bg` dark — no light-mode leak
- [ ] Status bar shows light content (white time + icons)
- [ ] No truncation / overflow / clipping
- [ ] Animations enter cleanly at 60fps (no jank)
- [ ] All copy is verbatim per UI-SPEC (where contracted) — no `Memori` user-facing string drift
- [ ] On Pain Cards: tap `Caught me` 4 times, `Not me` 2 times → receiptCount = 4
- [ ] On Plan Reveal: receipt-aware line reads `You admitted to 4 feed loops. Memo goes after those first.`
- [ ] Page dots HIDDEN on Empathy + Plan Reveal
- [ ] Plan Reveal CTA `Start my training` advances to paywall
- [ ] Commitment hold-to-agree completes → mainTabView

**Walk B — N=0 receipt run, light mode (iOS Settings → Display & Brightness → Light):**

Fresh-install. Walk again, this time:
- iOS in light mode (system-wide).
- On Pain Cards, tap `Not me` on all 6 prompts → receiptCount = 0.
- On Plan Reveal, verify receipt-aware line reads: `Memo builds the plan around your goals.`

For every page, confirm:
- [ ] Page renders DARK (`.preferredColorScheme(.dark)` pinned — light mode iOS does NOT bleed into Memo onboarding)
- [ ] Status bar still reads light content
- [ ] No modal/sheet/popover bleeds light-mode background
- [ ] Keyboard on Name page is dark

**Walk C — Reduce Motion ON, dark mode:**

iOS Settings → Accessibility → Motion → Reduce Motion ON. Fresh-install. Walk through.

Verify (on the 6 polished pages: Pain Cards / Goals / Industry Scare / Plan Reveal / Commitment / Screen Time / Empathy if polished):
- [ ] Entrance animations are opacity-fade only (no springs / scales / slides)
- [ ] Plan Reveal count-up SKIPPED — final 22,000 shown immediately
- [ ] Pain Cards `Caught me` stamp shows at full scale (no scale-pop), `Not me` slip fades out (no flick)
- [ ] Goals card selection has NO lift/glow — only border + dot color change
- [ ] Haptics still fire on every page where they should

**Specific checks:**
- iPhone SE 3 (375pt frame) — if a second device is available, repeat the walk. If not, use the iOS Simulator's iPhone SE 3 frame as a sanity check (run `xcodebuild test -destination 'platform=iOS Simulator,name=iPhone SE (3rd generation)'` or use Xcode's Simulator app to launch on SE).
- VoiceOver: turn on (Settings → Accessibility → VoiceOver) on at least 2–3 pages (Pain Cards, Goals, Plan Reveal). Verify pages read in logical order.

**Document:**
For each page, mark PASS / FAIL with notes. If any FAIL, decide: in-scope fix in this plan, or deferred to a future polish phase / Phase 3 BRAND-05 (for `Memori` strings).
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 pages pass × all 3 walks. Or list specific page + checklist-item failures — Claude will iterate (max 1 more pass, otherwise create a follow-up plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Sweep + verify only. ONB-01/02/03 are already shipped (no new threats). ONB-08 is a parity audit (no new code paths). ONB-09 is plumbing verification (already in Plan 01 + Plan 04 threat models). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information Disclosure | iOS light-mode bleed (theoretical) | accept | Audit verifies `.preferredColorScheme(.dark)` is pinned. No PII at risk; the worst-case is a brief flash of system-light chrome on a status bar — cosmetic only. |

**Severity: low/none — verification pass + minor color-leak fixes only. No permission changes, no network, no persistent storage of sensitive data.**
</threat_model>

<verification>
- ONB-01/02/03 confirmed shipped (commit `c5a73ac`).
- ONB-08 checklist completed for all 16 pages × 3 walks (dark / light / Reduce Motion).
- ONB-09 receipt-count plumbing verified end-to-end with N=4 and N=0 runs.
- All 6 polished pages from Plans 01–07 confirmed working as a flow.
- `xcodebuild` succeeds on device target.
- No `AppColors.*` references in onboarding source files (or, if found, replaced with OB tokens).
- No raw `Color.white` / `Color.black` fills (opacity-modified on white is fine for OB.border etc.).
</verification>

<success_criteria>
- All 16 pages × ONB-08 checklist items PASS
- ONB-09 verified end-to-end on device
- ONB-01/02/03 acknowledged as shipped
- Phase 1 ready to mark complete
- All polish-phase commits in main / v2.0-focus-mode branch
- User signs off on the final device walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-final/01-08-SUMMARY.md` per `templates/summary.md`. Include:
- Static grep audit results (Task 1)
- Defect fix list (Task 2 — or NO-OP)
- Per-page ONB-08 checklist results (Task 3 Walk A + Walk B)
- ONB-09 verification results (N=4 + N=0 runs)
- Reduce Motion verification (Walk C)
- ONB-01/02/03 explicit acknowledgment
- Any items deferred to Phase 3 BRAND-05 (`Memori` user-facing strings)
- Final phase status: ready to mark complete via `/gsd-transition` or equivalent
</output>
