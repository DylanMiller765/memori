---
phase: 01-onboarding-final
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - MindRestore/Views/Onboarding/OnboardingNewScreens.swift
autonomous: false
requirements: [ONB-07, ONB-09]
must_haves:
  truths:
    - "Plan Reveal Layer 1 ('Without Memo') is full-bleed (no horizontal margin) with OB.coral atmosphere tint"
    - "Plan Reveal Layer 2 ('With Memo') is full-bleed with OB.accent atmosphere tint"
    - "Hero numbers (44,000 and 22,000) render in `.system(... design: .monospaced)` at 88–96pt — NOT brand font"
    - "Count-up animates 0 → 44,000 over ~1.8s on entrance with light haptic ticks"
    - "Crossfade 44,000 → 22,000 fires after Layer 1 settles, with medium haptic on settle"
    - "Receipt-aware line varies by `receiptCount`: '> 0' renders 'You admitted to {N} feed loops. Memo goes after those first.'; '== 0' renders 'Memo builds the plan around your goals.'"
    - "Plan card has exactly 4 rows (DAILY TRAINING / APPS LOCKED / DAILY UNLOCKS EARNED / WEEKLY LEADERBOARD) — no 3-row+footnote alternative"
    - "Plan card header reads 'The plan.' — NOT 'Your Prescription' / 'RX-072' / 'The Memori Protocol'"
    - "Page dots are HIDDEN on Plan Reveal"
    - "No floating App Store testimonial card under the layers"
    - "Reduce Motion mode shows final 22,000 immediately with opacity-only fade-ins"
  artifacts:
    - path: "MindRestore/Views/Onboarding/OnboardingNewScreens.swift"
      provides: "Polished `OnboardingPersonalSolutionView` matching UI-SPEC §'Page 11 — Plan Reveal'"
      contains: "struct OnboardingPersonalSolutionView"
  key_links:
    - from: "OnboardingPersonalSolutionView.receiptCount"
      to: "Receipt-aware line copy"
      via: "if receiptCount > 0 ? variant A : variant B"
      pattern: "receiptCount"
    - from: "OnboardingView.painCardsPage onContinue closure"
      to: "OnboardingView @State receiptCount"
      via: "callback assigns count to @State"
      pattern: "receiptCount = count"
    - from: "OnboardingView state receiptCount"
      to: "OnboardingPersonalSolutionView init"
      via: "parameter pass at construction site"
      pattern: "receiptCount: receiptCount"
---

<objective>
Polish the Plan Reveal page (page 11) per CONTEXT D-03 and UI-SPEC §"Page 11 — Plan Reveal." Targeted tweaks per audit, NOT a rewrite. The visual contract is locked: full-bleed Layer 1+2, mono hero numbers, count-up + crossfade, receipt-aware line driven by `receiptCount` from Pain Cards (ONB-09), 4-row plan card, hidden page dots.

Also verify ONB-09 receipt-count plumbing end-to-end: Pain Cards `Caught me` taps increment `OnboardingView.receiptCount` via the `onContinue: (Int) -> Void` callback, and `OnboardingPersonalSolutionView` receives it via the `receiptCount: Int` parameter (already wired per CONTEXT — confirm no regression).

Purpose: Plan Reveal is the climax of the onboarding flow. The 0→44k→22k cinematic moment is what converts the user emotionally before the paywall lands. If Layer 1+2 are too contained or numbers jitter, the climax falls flat.

Output: Revised `OnboardingPersonalSolutionView` matching UI-SPEC visual spec table + animation timeline + acceptance criteria. End-to-end receipt-count plumbing verified on device with a Pain Cards run that produces N≥1 receipts and confirms the Plan Reveal copy reads correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-onboarding-final/01-CONTEXT.md
@.planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md
@.planning/phases/01-onboarding-final/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/BRAND.md
@MindRestore/Views/Onboarding/OnboardingNewScreens.swift

<interfaces>
<!-- Existing struct signature (verified from on-disk read at OnboardingNewScreens.swift line 115) -->

```swift
struct OnboardingPersonalSolutionView: View {
    let userGoals: Set<UserFocusGoal>
    let brainAge: Int?
    let userAge: Int
    let dailyScreenTimeHours: Double
    let projectedScreenTimeHours: Int
    let projectionIsEstimate: Bool
    let receiptCount: Int                  // ALREADY WIRED — DO NOT CHANGE SIGNATURE
    let onContinue: () -> Void

    // Existing private @State for animation beats:
    private enum RevealBeat { case stakes; case withMemo; case plan }
    @State private var cardsAppeared: [Bool] = [false, false, false, false]
    @State private var revealBeat: RevealBeat = .stakes
    @State private var headlineAppeared = false
    @State private var animatedProjectionHours = 0
    @State private var animatedReclaimedHours = 0
    @State private var revealStarted = false
    @State private var revealTask: Task<Void, Never>?
}
```

OB tokens: `OB.bg`, `OB.surface`, `OB.coral`, `OB.accent`, `OB.fg`, `OB.fg2`, `OB.fg3`, `OB.border`, `OB.success` (defined OnboardingNewScreens.swift line 1312).

Existing call sites (verified from on-disk read at OnboardingNewScreens.swift lines 1236, 1249 — these are #Preview blocks):
```swift
OnboardingPersonalSolutionView(
    userGoals: ..., brainAge: ..., userAge: ..., dailyScreenTimeHours: ...,
    projectedScreenTimeHours: ..., projectionIsEstimate: ...,
    receiptCount: 4,                       // ← already passing receiptCount in previews
    onContinue: {}
)
```

Real call site is in `OnboardingView.swift` (search for `OnboardingPersonalSolutionView(` to find the planRevealPage construction). It must pass `receiptCount: receiptCount` (where the right side is the `@State` declared at line 51 of OnboardingView.swift).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Polish `OnboardingPersonalSolutionView` to match UI-SPEC §"Page 11 — Plan Reveal"</name>
  <read_first>
    - MindRestore/Views/Onboarding/OnboardingNewScreens.swift (read lines 115–500 — entire `OnboardingPersonalSolutionView` body, including its `RevealBeat` state machine, count-up logic, and plan-card row rendering)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (§"Page 11 — Plan Reveal" lines 632–741 — layout sketch, visual spec table, animation timeline, accessibility, acceptance criteria)
    - .planning/phases/01-onboarding-final/01-CONTEXT.md (D-03 — targeted tweaks, NOT rewrite; D-10 — receipt count flow; mention that Codex impl is mostly there)
    - .planning/phases/01-onboarding-final/01-01-SUMMARY.md (Plan 01 confirms `receiptCount` callback signature preserved)
    - MindRestore/Views/Onboarding/OnboardingView.swift (find the `OnboardingPersonalSolutionView(...)` call site to confirm `receiptCount: receiptCount` is wired)
  </read_first>
  <files>MindRestore/Views/Onboarding/OnboardingNewScreens.swift</files>
  <action>
Audit the current `OnboardingPersonalSolutionView` against the UI-SPEC §"Page 11 — Plan Reveal" acceptance criteria (lines 724–739). For each criterion that fails, apply a targeted fix. Do NOT rewrite the struct.

**Fix A — Full-bleed Layer 1 + Layer 2:**
- Find the Layer 1 background view (likely a `RoundedRectangle` or VStack with `.background(OB.coral.opacity(...))`).
- Ensure horizontal padding is ZERO on the layer container (`.frame(maxWidth: .infinity)` then `.background(OB.coral.opacity(0.12))`). The number and headline INSIDE the layer keep their 28pt leading margin, but the colored tint extends edge-to-edge.
- Same for Layer 2 with `OB.accent.opacity(0.18)`.
- Per UI-SPEC visual spec table:
  - Layer 1 background: `OB.coral.opacity(0.12)` full-bleed gradient, no horizontal margin
  - Layer 1 height: `min(40% of viewport, 320pt)` — number must dominate
  - Layer 2 background: `OB.accent.opacity(0.18)` full-bleed
  - Layer 2 height: matches Layer 1

**Fix B — Mono hero numbers at 88–96pt:**
- Find the count-up Text (currently rendering `animatedProjectionHours` and `animatedReclaimedHours`).
- Set the font: `.system(size: 92, weight: .heavy, design: .monospaced)`.
- If currently using `.brand(...)`, REPLACE — proportional digits jitter during count-up.
- Foreground for Layer 1 number: `OB.fg`. Layer 2 number: `OB.accent` (or `OB.success` per UI-SPEC if accent feels too cool/neutral — D-03 leaves this to executor judgment if user testing flags it).

**Fix C — Layer 1 eyebrow + headline + unit:**
- Layer 1 eyebrow: `WITHOUT MEMO`, `.brand(size: 13, weight: .bold)`, tracking 1.0, `OB.fg3`, top-left padding (top 28pt, leading 28pt). Use `OBEyebrow(text: "WITHOUT MEMO")` if the spacing matches.
- Layer 1 headline: `Here's what's at stake.`, `.brand(size: 22, weight: .bold)`, `OB.fg`, leading 28pt.
- Layer 1 unit (under the number): `HOURS · 5 YEARS OF YOUR LIFE`, `.system(size: 12, weight: .heavy, design: .monospaced)`, tracking 1.5, `OB.fg2`, centered horizontally below number.

**Fix D — Layer 2 eyebrow + headline + unit:**
- Eyebrow: `WITH MEMO`, same treatment as Layer 1, `OB.fg3`.
- Headline: `Memo cuts this in half.`, `.brand(size: 22, weight: .bold)`, `OB.fg`.
- Unit: `HOURS · 4 YEARS BACK`.

**Fix E — Receipt-aware line (CRITICAL — ONB-09):**
- Below Layer 2 (or above the plan card, per UI-SPEC layout sketch), add a single line that reads conditionally based on `receiptCount`:

```swift
private var receiptAwareLine: String {
    if receiptCount > 0 {
        return "You admitted to \(receiptCount) feed loop\(receiptCount == 1 ? "" : "s"). Memo goes after those first."
    } else {
        return "Memo builds the plan around your goals."
    }
}
```

Wait — UI-SPEC §"Page 11 — Plan Reveal" copywriting table specifies the receiptCount==0 fallback as `Memo builds the plan around your goals.` (line 224). The Layer 1 eyebrow `if receiptCount > 0` line uses `feed loops` plural with no singular/plural switch documented. To be safe, default to plural per UI-SPEC verbatim:
- `receiptCount > 0`: `You admitted to \(receiptCount) feed loops. Memo goes after those first.`
- `receiptCount == 0`: `Memo builds the plan around your goals.`

(Verbatim per UI-SPEC line 224. Do NOT add singular/plural variants — keep `feed loops` as-is per spec.)

Render with `.brand(size: 14, weight: .semibold)`, `OB.fg2`, `28pt` page horizontal margin, multi-line OK with `.fixedSize(horizontal: false, vertical: true)`.

**Fix F — Plan card structure: LOCKED to 4 rows:**

Per UI-SPEC §"Page 11" copywriting table + visual spec table, the plan card MUST be exactly 4 rows in this exact order:
- `01 · DAILY TRAINING` → `5 min`
- `02 · APPS LOCKED` → `\(focusGoals.count)` (default `4` if user hasn't picked yet — read from `userGoals.count` in this view; fallback to 4 if 0)
- `03 · DAILY UNLOCKS EARNED` → `3×`
- `04 · WEEKLY LEADERBOARD` → `unlocks Day 5`

Card header: `The plan.` (NOT `Your Prescription` / `RX-072` / `The Memori Protocol` — these are explicitly forbidden per UI-SPEC anti-pattern guards line 234).

Card style:
- Fill: `OB.surface`
- Page margin: 28pt horizontal
- Corner radius: 22pt
- Border: `OB.border` 1pt
- Header `The plan.`: `.brand(size: 17, weight: .heavy)`, `OB.fg`, top padding 18pt, leading 18pt
- Row eyebrow `01 · DAILY TRAINING`: `.system(size: 11, weight: .heavy, design: .monospaced)`, tracking 1.2, `OB.fg3`
- Row value `5 min`: `.system(size: 17, weight: .bold, design: .monospaced)`, `OB.fg`, trailing-aligned
- Row hairline rule between rows: `Color.white.opacity(0.08)`, 1pt
- Row vertical padding: 14pt

If the current impl has a 3-row + footnote layout, REMOVE the footnote and add the 4th row. If it has 4 rows already but the values drift from the spec, fix the strings verbatim.

**Fix G — Page dots HIDDEN:**

Plan Reveal must hide the parent's page-dots progress indicator. Per UI-SPEC line 805, `OnboardingView` (parent) hides progress on page indices 6 (Empathy) and 11 (Plan Reveal) via a `progressVisible` flag. Verify in `OnboardingView.swift` that the page-dots renderer hides on `currentPage == 11`. If not, this fix may need to extend into `OnboardingView.swift` — BUT this plan's `files_modified` is `OnboardingNewScreens.swift` only (Plan 03 owns OnboardingView for Wave 1, Plan 05/06/07 for Wave 2/3). To avoid file-conflict, INSPECT `OnboardingView.swift` first; if the dots are already hidden on page 11, no edit needed. If not, FLAG to the user during checkpoint and propose adding the hide-condition in Plan 08 (sweep) instead of widening this plan's scope.

**Fix H — Forbidden chrome (delete on sight):**

UI-SPEC anti-pattern guards (lines 234–238):
- No `RX-072 v.1` / `Your Prescription` / `The Memori Protocol` strings
- No version numbers, Rx glyphs, signature lines, fake-medical-chart aesthetic
- No floating App Store testimonial card under the layers (testimonial belongs on the paywall)
- No page dots on Plan Reveal
- No second progress system competing with the main progress bar

Search the entire `OnboardingPersonalSolutionView` for any of these patterns and remove. The user has explicitly rejected medical-LARP framing across rounds.

**Fix I — Animation timeline (UI-SPEC §"Page 11" animation table):**

Verify (or set) the entrance arc:
- t=0.10s: Layer 1 background tints in (opacity 0→1, 0.40s easeOut)
- t=0.20s: Layer 1 eyebrow + headline fade in
- t=0.40s: Number count-up begins: 0 → 44,000 over 1.8s, mono digits, light haptic intensity 0.3 every 50 integers (~36 ticks total)
- t=1.40s: Layer 2 background tints in
- t=2.20s: Number crossfade: 44,000 → 22,000 over 0.6s with mono digit slide; medium haptic on settle
- t=2.85s: Receipt-aware line + plan card + CTA fade up (staggered 0.10s, easeOut)

The existing `revealTask` Task and `animatedProjectionHours`/`animatedReclaimedHours` state likely already implement most of this. Verify timing and adjust durations to match.

**Fix J — Reduce Motion fallback:**

Add (or verify) `@Environment(\.accessibilityReduceMotion) private var reduceMotion`.

When `reduceMotion == true`:
- Skip the count-up. Set `animatedProjectionHours = 44_000` and `animatedReclaimedHours = 22_000` immediately on appear.
- Replace background tint-ins, headline rises, and crossfade with 0.18s opacity fades.
- Keep the medium haptic at the moment Layer 2 would have settled (single fire on appear after a small delay so it still feels timed to the user's reading).

**Fix K — CTA:**
- `OBContinueButton(title: "Start my training", action: onContinue)`
- 28pt horizontal margin, 24pt above bottom safe-area inset

**Build & verify:**
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -10
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"WITHOUT MEMO"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 1 eyebrow
    - `grep -nE '"WITH MEMO"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 2 eyebrow
    - `grep -nE '"Here.s what.s at stake\\."' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 1 headline
    - `grep -nE '"Memo cuts this in half\\."' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 2 headline
    - `grep -nE '"HOURS · 5 YEARS OF YOUR LIFE"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 1 unit
    - `grep -nE '"HOURS · 4 YEARS BACK"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Layer 2 unit
    - `grep -nE '"The plan\\."' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the plan card header
    - `grep -nE '"You admitted to .* feed loops"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the receipt-aware line variant A
    - `grep -nE '"Memo builds the plan around your goals\\."' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the receipt-aware line variant B
    - `grep -nE '"Start my training"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the CTA
    - `grep -nE '"01 · DAILY TRAINING"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds row 01
    - `grep -nE '"02 · APPS LOCKED"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds row 02
    - `grep -nE '"03 · DAILY UNLOCKS EARNED"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds row 03
    - `grep -nE '"04 · WEEKLY LEADERBOARD"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds row 04
    - `grep -nE 'RX-072|Prescription|Memori Protocol|Your Prescription' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` returns NO matches
    - `grep -n "design: .monospaced" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds at least 3 hits inside `OnboardingPersonalSolutionView` (numbers, units, row eyebrows, row values)
    - `grep -n "accessibilityReduceMotion" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the binding inside `OnboardingPersonalSolutionView`
    - `xcodebuild` returns `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>
    - All UI-SPEC §"Page 11" acceptance criteria pass on device
    - Receipt-count plumbing verified end-to-end (Pain Cards → OnboardingView state → Plan Reveal)
    - Forbidden chrome (RX-072 / Prescription / Protocol / testimonial card) removed if present
    - Build + install succeed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Plan Reveal on iPhone (with N=4 receipt run)</name>
  <what-built>Polished `OnboardingPersonalSolutionView` matching UI-SPEC §"Page 11 — Plan Reveal" line-for-line: full-bleed Layer 1+2, mono numbers, count-up + crossfade, receipt-aware line, 4-row plan card, hidden page dots, Reduce Motion fallback. ONB-09 plumbing verified end-to-end.</what-built>
  <how-to-verify>
On iPhone, do TWO test runs:

**Run A — N=4 receipt count:**
1. Fresh-install (delete app first to reset onboarding).
2. Advance through onboarding. On Pain Cards, tap `Caught me` on prompts 1, 2, 3, 4 and `Not me` on prompts 5, 6 (so receiptCount = 4).
3. Continue through Empathy → Goals → Age → Screen Time → Personal Scare → Quick Assessment → arrive at Plan Reveal.
4. Verify Layer 1 ("Without Memo") appears full-bleed with coral tint.
5. Verify count-up animates 0 → 44,000 in mono digits over ~1.8s. Light haptic ticks during count.
6. Verify Layer 2 ("With Memo") appears full-bleed with accent tint.
7. Verify crossfade 44,000 → 22,000 with medium haptic on settle.
8. Verify receipt-aware line reads exactly: `You admitted to 4 feed loops. Memo goes after those first.`
9. Verify plan card has exactly 4 rows in correct order with values: 5 min / 4 / 3× / unlocks Day 5 (the `4` for APPS LOCKED reflects Goals selections — if user picked 3 goals, value reads `3`).
10. Verify card header reads `The plan.` — NOT prescription / RX / protocol.
11. Verify NO page dots visible.
12. Verify NO floating testimonial card under layers.
13. Tap `Start my training` — verify advance to v9-finch paywall (existing behavior).

**Run B — N=0 receipt count:**
14. Fresh-install. Tap `Not me` on all 6 Pain Cards prompts (so receiptCount = 0).
15. Advance to Plan Reveal.
16. Verify receipt-aware line reads exactly: `Memo builds the plan around your goals.`
17. All other Plan Reveal layout matches Run A.

**Reduce Motion check:**
18. Enable Reduce Motion in iOS Settings → Accessibility → Motion.
19. Re-run Plan Reveal.
20. Verify count-up SKIPPED — final 22,000 shown immediately with a clean opacity fade.
21. Verify medium haptic still fires at the cinematic moment.
  </how-to-verify>
  <resume-signal>Type "approved" or describe defects (wrong copy, page dots visible, count-up jitter, missing haptic, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `receiptCount` parameter → conditional copy | Receipt count flows from Pain Cards through `OnboardingView.receiptCount` @State to `OnboardingPersonalSolutionView` parameter. In-memory only, never persisted, never transmitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | `receiptCount` value rendered in copy | accept | A small integer (0–6) embedded in onboarding copy. No PII. No diagnostic value to attacker. |
| T-01-06 | Denial of Service | Count-up animation + Task lifecycle | accept | Existing `revealTask: Task<Void, Never>?` is canceled `onDisappear`. No retain cycle if `[weak self]` capture is preserved (executor: verify during edit). |

**Severity: low/none — UI polish only, no permission changes, no network, no persistent storage of sensitive data.**
</threat_model>

<verification>
- `xcodebuild` succeeds.
- All 12 UI-SPEC §"Page 11" acceptance criteria pass on device for both N=4 and N=0 runs.
- ONB-09 receipt-count plumbing confirmed working end-to-end on a real Pain Cards run.
- Reduce Motion fallback works.
- No forbidden chrome remains (RX / Prescription / Protocol / testimonial card / page dots).
</verification>

<success_criteria>
- Plan Reveal lands at climax-moment polish: full-bleed layers, mono numbers, count-up + crossfade, receipt-aware copy
- Both receipt-count variants render correctly
- 4-row plan card structure locked
- Build + install succeed
- Human-verified on iPhone for both N=4 and N=0 paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-final/01-04-SUMMARY.md` per `templates/summary.md`. Note any minor sub-fixes that ended up needed in `OnboardingView.swift` (e.g. page-dots hide condition) — Plan 08 (sweep) will pick those up if they don't fit here.
</output>
