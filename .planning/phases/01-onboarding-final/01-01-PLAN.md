---
phase: 01-onboarding-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MindRestore/Views/Onboarding/OnboardingNewScreens.swift
autonomous: false
requirements: [ONB-04]
must_haves:
  truths:
    - "Pain Cards page background is dark OB.bg — no cream paper card surface"
    - "Micro progress reads as '3 of 6' (lowercase brand font, not monospaced caps) — the '0F' misread is gone"
    - "Saved-receipt back-stack is visibly readable (3 layers stacked under the front slip with rotations and offsets per spec)"
    - "Mascot peeks from bottom-left without overlapping confession text or the action buttons"
    - "Perforation renders on the TOP edge of the front receipt, not the middle"
    - "Tapping 'Caught me' triggers the CAUGHT stamp animation in the lower-right quadrant of the active slip; tapping 'Not me' does not"
    - "Empty back-slip body is blank (no 'saved receipt'/'feed loop' filler text)"
    - "Reduce Motion mode swaps the slide/flick animations for 0.18s opacity fades; haptics still fire"
  artifacts:
    - path: "MindRestore/Views/Onboarding/OnboardingNewScreens.swift"
      provides: "Polished OnboardingPainCardsView struct with fixed back-stack opacity, top-edge perforation, lowercase micro-progress, and bottom-left mascot peek"
      contains: "struct OnboardingPainCardsView"
  key_links:
    - from: "OnboardingPainCardsView"
      to: "ReceiptPerforation shape (TOP edge of front slip)"
      via: "Path.move(to:) at y=0 inset 16pt; dotted dash pattern [2,5]"
      pattern: "ReceiptPerforation"
    - from: "OnboardingPainCardsView final tap"
      to: "onContinue(receiptCount: Int)"
      via: "callback fires when 6th prompt resolves"
      pattern: "onContinue\\("
---

<objective>
Polish the Pain Cards page (page 2 of onboarding) so it lands at OB-system polish on a clean device. Fix the 6 known defects from the first Codex pass: invisible back-stack, illegible "1 OF 6" monospaced label, mascot clipping, mid-slip perforation, airy receipt sizing, and meaningless ambient back-slip filler.

Purpose: Pain Cards is the first emotional moment in the v2.0 onboarding flow. Per CONTEXT D-01 and UI-SPEC §"Page 2 — Pain Cards", these 6 fixes are non-negotiable polish targets — the visual contract is locked, this plan only edits the Swift code that renders it.

Output: A revised `struct OnboardingPainCardsView` in `OnboardingNewScreens.swift` whose rendering matches the UI-SPEC visual spec table line-for-line, plus the verbatim copywriting contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-onboarding-final/01-CONTEXT.md
@.planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@docs/BRAND.md
@MindRestore/Views/Onboarding/OnboardingNewScreens.swift

<interfaces>
<!-- Existing tokens / components the executor MUST use directly. Do not redeclare. -->

From OnboardingNewScreens.swift (line 1312):
```swift
enum OB {
    static let bg       = Color(red: 0.039, green: 0.039, blue: 0.060)   // #0A0A0F
    static let surface  = Color(red: 0.078, green: 0.078, blue: 0.121)   // #14141F
    static let border   = Color.white.opacity(0.08)
    static let fg       = Color.white.opacity(0.94)
    static let fg2      = Color.white.opacity(0.62)
    static let fg3      = Color.white.opacity(0.40)
    static let accent   = Color(red: 0.408, green: 0.565, blue: 0.996)   // #6890FE
    static let coral    = Color(red: 0.980, green: 0.420, blue: 0.349)   // #FA6B59
    // memoPurple, success, amber as documented
}
```

From OnboardingNewScreens.swift (line 1326):
```swift
struct OBEyebrow: View { /* eyebrow with .brand(size: 13, weight: .bold) tracking 1.0 */ }
```

From OnboardingNewScreens.swift (line 1337):
```swift
struct OBContinueButton: View { /* primary CTA, OB.accent fill, 14pt corner radius */ }
```

From DesignSystem.swift (line ~579):
```swift
extension Font { static func brand(size: CGFloat, weight: Font.Weight = .regular) -> Font }
```

Existing struct signature (do NOT change the public surface):
```swift
struct OnboardingPainCardsView: View {
    let onContinue: (Int) -> Void   // receiptCount param already wired into OnboardingView.painCardsPage
    // private @State for index, savedReceipts, etc.
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply the 6 Pain Cards polish fixes per CONTEXT D-01 and UI-SPEC §"Page 2 — Pain Cards"</name>
  <read_first>
    - MindRestore/Views/Onboarding/OnboardingNewScreens.swift (entire file, focusing on lines 1360–1777 — `OnboardingPainCardsView` and `ReceiptPerforation`)
    - .planning/phases/01-onboarding-final/01-onboarding-final-UI-SPEC.md (especially §"Page 2 — Pain Cards" lines 146–471, including the layout sketch, visual spec table, animation timeline, accessibility, and acceptance criteria sections)
    - .planning/phases/01-onboarding-final/01-CONTEXT.md (D-01 enumerates the exact 6 issues to fix)
    - docs/BRAND.md (voice rules: no interrogation phrasing, no "QUESTION 03/06" chrome)
  </read_first>
  <files>MindRestore/Views/Onboarding/OnboardingNewScreens.swift</files>
  <action>
Edit `struct OnboardingPainCardsView` (line ~1360) and `struct ReceiptPerforation` (line ~1777) so the rendered page matches UI-SPEC line-for-line. Make these specific changes:

**Fix 1 — Micro progress label "1 OF 6" → "3 of 6" lowercase brand:**
- Find any `Text(...)` rendering the prompt index that uses `.system(..., design: .monospaced)` and `.textCase(.uppercase)`.
- Replace with: `Text("\(currentIndex + 1) of 6").font(.brand(size: 11, weight: .semibold)).foregroundStyle(OB.fg3)` — NO `.textCase(.uppercase)`, NO monospaced design. This kills the "0F" misread (D-01a).

**Fix 2 — Back-stack opacity bump:**
- Find the array of saved-receipt opacities (currently `[0.54, 0.34, 0.22]` per CONTEXT, but verify on disk — Codex impl may have shipped `[0.05, ...]` or similar invisible values).
- Set them to exactly `[0.78, 0.55, 0.35]` for back slips 1, 2, 3 respectively. Per UI-SPEC visual spec table:
  - Back slip 1: `rotation +5°, y +18, x +10, opacity 0.78`
  - Back slip 2: `rotation -4°, y +36, x -8, opacity 0.55`
  - Back slip 3 (only if ≥3 caught): `rotation +8°, y +54, x +14, opacity 0.35`
- Cap visible saved slips at 3 (per UI-SPEC line ~431: "Back slip 3 (only if ≥3 caught)").

**Fix 3 — Perforation moves to TOP edge:**
- In `ReceiptPerforation: Shape` (line ~1777), modify `path(in:)` so the dashed line draws at `y = 0` (top edge inset 16pt from leading and trailing) instead of the middle of the rect.
- Use dash pattern `[2, 5]` with stroke color `Color.white.opacity(0.14)`, stroke width `1pt`.
- The receipt body MUST sit BELOW the perforation, not be split by it. Verify by reading the front-slip composition — perforation should be the first child after the rounded rectangle background, anchored to top.

**Fix 4 — Mascot peek (no clipping):**
- Locate the mascot Image rendering `mascot-thinking` (search for `mascot-thinking`).
- Set frame: `.frame(height: 96)` with `.aspectRatio(contentMode: .fit)`.
- Anchor: `.frame(maxWidth: .infinity, alignment: .bottomLeading)` with horizontal leading padding `28pt`.
- DROP any `.offset(y: 58)` that pushes it off-screen. Replace with `.offset(y: 18)` max — and ONLY if the parent VStack/ZStack has been extended to accommodate it. The mascot must NEVER cover the active confession text or the action buttons. Per UI-SPEC: "may tuck behind stack to show only head/glasses/one hand on small screens" — acceptable for it to overlap the back-stack area, NOT the front slip body or buttons.

**Fix 5 — Receipt sizing (drop airiness):**
- In the front-slip body, find any `minHeight: 210` paired with double `Spacer()` calls that pad the content vertically.
- Keep `.frame(minHeight: 210)` (per UI-SPEC line ~427) BUT remove the trailing `Spacer()` that was pushing content to the top — let the confession text center naturally inside the 18pt vertical padding. Per UI-SPEC visual spec table: "Front receipt padding: 18pt H × 18pt V".

**Fix 6 — Empty back-slip ambient text:**
- Find the back-slip rendering (the dimmed slips behind the front). If they currently render filler body text like `Text("saved receipt")` or `Text("feed loop")` as their body, REMOVE those Text views entirely.
- Back slips render ONLY the dim slip background + perforation + the small `saved receipt` label (per UI-SPEC line ~152). Their body area is BLANK. The user's previously-caught confession text does NOT carry forward into the back slip.
- Specifically: the back-slip composition should be `RoundedRectangle(16) → ReceiptPerforation (top edge) → Text("saved receipt").font(.brand(size: 12, weight: .medium)).foregroundStyle(OB.fg3) → Spacer()`. No body text below the label.

**Verbatim copy contract (UI-SPEC §"Page 2 — Pain Cards" copywriting table):**
Confirm or set these strings exactly. Replace any divergent copy:
- Eyebrow: `MEMO FOUND THE RECEIPTS`
- Headline: `Which ones are yours?`
- Subcopy: `Tap what feels painfully familiar. Memo uses it to build your fight plan.`
- Active label: `current receipt` (lowercase, brand 12pt medium, OB.fg3)
- Saved label: `saved receipt` (on dim back slips)
- Pain statement 1: `I check my phone before I check the time`
- Pain statement 2: `I forget what I just read on a page`
- Pain statement 3: `I uninstall TikTok, then redownload by Friday`
- Pain statement 4: `I scroll until 2am even when I know better`
- Pain statement 5: `I open the same 4 apps in a loop`
- Pain statement 6: `I can't sit through a movie without my phone`
- Primary CTA: `Caught me`
- Secondary CTA: `Not me`
- Stamp text: `CAUGHT` (mono 22pt heavy, tracking 1.8, OB.coral, rotation -8°, lower-right quadrant)

**Forbidden chrome (delete on sight):**
- No `QUESTION 03/06` or `QUESTION X/Y` label anywhere.
- No "Yep" / "Nah" buttons — only `Caught me` / `Not me`.
- No interrogation phrasing in subcopy.
- No cream / paper-tinted card backgrounds — front slip fill MUST be `OB.surface`.

**Animation timeline (UI-SPEC §"Page 2 — Pain Cards" animation table):**
- 0.10s: Header fades up (opacity 0→1, y 8→0, 0.38s easeOut)
- 0.32s: Receipt stack rises (opacity 0→1, y 24→0, spring response 0.50, damping 0.82)
- 0.48s: Memo peeks bottom-left (opacity 0→1, scale 0.90→1, rotation -7°→-4°, spring response 0.46, damping 0.80)
- 0.78s: Buttons fade up (opacity 0→1, y 10→0, 0.30s easeOut)

**Per-tap (UI-SPEC §"Animation Cadence Pattern"):**
- `Caught me`: stamp scale 0.72→1.08→1.0 over 0.18s, hold 0.18s, slide back with rotation +5°. Medium haptic on tap (before stamp animation).
- `Not me`: rotation -9°, x offset -340pt, opacity → 0 over 0.30s. Light haptic on tap.

**Reduce Motion fallback (UI-SPEC §"Animation Cadence Pattern" + §"Per-Page Specs / Accessibility"):**
- Add `@Environment(\.accessibilityReduceMotion) private var reduceMotion` to `OnboardingPainCardsView`.
- When `reduceMotion == true`:
  - Replace the slide/flick on `Not me` with a 0.18s opacity fade to 0 in place.
  - Replace the stamp scale-pop on `Caught me` with a 0.18s opacity fade-in at full scale.
  - Replace entrance springs (receipt rise, mascot peek) with 0.18s opacity fades.
- Haptics MUST still fire in Reduce Motion mode.

**Receipt-count plumbing (D-10 already wired — DO NOT regress):**
- Confirm `let onContinue: (Int) -> Void` is the public init param.
- Confirm the final `Caught me` tap on prompt index 5 calls `onContinue(receiptCount)` where `receiptCount` is the running count of `Caught me` taps across all 6 prompts.
- Do NOT change this signature — Plan 04 (Plan Reveal) and `OnboardingView.painCardsPage` already consume it.

**Visual spec values to copy verbatim into the code (UI-SPEC §"Page 2" table):**
- Front receipt width: page width − 48pt horizontal margin
- Front receipt min-height: `210pt`
- Front receipt corner radius: `16pt`
- Front receipt padding: `18pt H × 18pt V`
- Front receipt fill: `OB.surface`
- Front receipt border: `OB.accent.opacity(0.45)`, `1.5pt`
- Confession text: `.brand(size: 22, weight: .heavy)`, `OB.fg`, `.lineSpacing(2)`
- CAUGHT stamp: `.system(size: 22, weight: .heavy, design: .monospaced)`, tracking `1.8`, `OB.coral`, `2pt` rounded-rect border at `0.72` opacity, rotation `-8°`
- Buttons gap: `12pt` between `Not me` and `Caught me`
- "Not me" style: `OB.surface` fill, `Color.white.opacity(0.10)` border, `OB.fg2` text
- "Caught me" style: `OBContinueButton` (which is OB.accent fill 14pt corner)

**Build & verify:**
After edits, build for device:
```
xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -20
```
Iterate on any compiler errors. SourceKit "Cannot find X" diagnostics are LIES per CLAUDE.md — only trust `xcodebuild` output.

If build succeeds, install and verify on device:
```
xcrun devicectl device install app --device 00008130-000A214E11E2001C build/Build/Products/Debug-iphoneos/MindRestore.app
```

Run the `/verify-changes` skill (per CLAUDE.md): build via xcodebuild, capture a screenshot if possible, show to user, explain what changed.
  </action>
  <verify>
    <automated>cd /Users/dylanmiller/Desktop/mindrestore && xcodebuild -project MindRestore.xcodeproj -scheme MindRestore -configuration Debug -destination 'id=00008130-000A214E11E2001C' -allowProvisioningUpdates -derivedDataPath build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "1 OF 6\|QUESTION 0\|QUESTION [0-9]\\/[0-9]" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` returns NO matches inside `OnboardingPainCardsView` (kill the uppercase / interrogation chrome)
    - `grep -nE "0\\.78|0\\.55|0\\.35" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` shows the new back-stack opacities are present in `OnboardingPainCardsView`
    - `grep -n "0\\.05\\|0\\.22\\|0\\.34\\|0\\.54" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` returns NO matches in the back-stack opacity array (old invisible values are gone)
    - `grep -n "saved receipt\\|feed loop" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` shows ONLY `"saved receipt"` as the dim back-slip label — no `"feed loop"` filler body
    - `grep -nE '"Caught me"|"Not me"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds both strings
    - `grep -nE '"Yep"|"Nah"' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` returns NO matches
    - `grep -nE 'minHeight: 210' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the front-slip min-height
    - `grep -n "accessibilityReduceMotion" MindRestore/Views/Onboarding/OnboardingNewScreens.swift` finds the Environment binding inside `OnboardingPainCardsView`
    - `grep -nE 'onContinue: \\(Int\\) -> Void' MindRestore/Views/Onboarding/OnboardingNewScreens.swift` confirms the receipt-count callback signature is preserved
    - `xcodebuild ...` returns `BUILD SUCCEEDED`
    - On-device manual verification: confession text never overlapped by the mascot at the smallest supported size (iPhone SE 3 frame, 375pt wide); CAUGHT stamp only animates on `Caught me` and only on the active slip's lower-right quadrant
  </acceptance_criteria>
  <done>
    - All 6 D-01 fixes applied with verbatim UI-SPEC values
    - `xcodebuild` succeeds on device target `00008130-000A214E11E2001C`
    - App installed on device; user has visually verified Pain Cards page on iPhone (light + dark)
    - `onContinue(Int)` plumbing untouched (Plan 04 verifies the downstream Plan Reveal consumes the value)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Pain Cards on iPhone</name>
  <what-built>Polished `OnboardingPainCardsView` with all 6 D-01 fixes (back-stack visibility, lowercase micro-progress, top-edge perforation, mascot peek, receipt sizing, empty back-slip body) plus Reduce Motion fallback and verbatim UI-SPEC copy.</what-built>
  <how-to-verify>
On iPhone (`00008130-000A214E11E2001C`), launch the app fresh from a clean install (delete + reinstall to reset onboarding):
1. Launch app, advance through Welcome → Name → land on Pain Cards.
2. Verify the page background is dark (`OB.bg`) — NO cream / off-white surface.
3. Verify the micro progress reads cleanly as `1 of 6` (lowercase brand, NOT `1 0F 6` monospaced).
4. Tap `Caught me` once — verify stamp animates in lower-right of the active slip, slip slides off, count increments.
5. Tap `Caught me` twice more — verify back-stack now shows 3 dim slips visibly stacked behind the front (NOT invisibly faded).
6. Verify the back slips are blank in the body — only the small `saved receipt` label appears, no filler text.
7. Verify the perforation dashed line is on the TOP edge of the front slip (looks like a torn-off coupon header), NOT bisecting the middle.
8. Verify Memo's mascot peeks from bottom-left, head/glasses visible, never covering the confession text or the buttons.
9. Tap `Not me` — verify slip flicks left and away with rotation, no count increment.
10. Complete all 6 prompts. Verify the page advances on the 6th tap.
11. Toggle iOS Settings → Accessibility → Motion → Reduce Motion ON. Re-launch. Verify entrance is fade-only and `Caught me` shows no scale-pop on the stamp — but the CAUGHT label still appears at full scale and a medium haptic still fires.
12. Verify in Settings → Display → Light Mode that the page still renders dark (`.preferredColorScheme(.dark)` pinned).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visible defect (over-fade, mascot clipping, wrong label, etc.) — Claude will iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (no auth, no network, no persistent storage of receipts) | The Pain Cards page renders entirely from in-memory `@State`. The `receiptCount` is passed via callback to `OnboardingView` (also in-memory `@State`). It is NEVER written to the `User` model, NEVER sent to any analytics endpoint with PII, NEVER persisted to disk. The 6 confession strings are hard-coded UI copy, not user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `receiptCount` ephemeral state | accept | Receipt-count is a pure UX signal (number 0–6) used only to vary downstream onboarding copy. No PII, no diagnostic value to an attacker. Lives in onboarding `@State` only. |

**Severity: low/none — UI polish only, no permission changes, no network, no persistent storage of sensitive data.**
</threat_model>

<verification>
- `xcodebuild` succeeds on device target.
- All 8 must-haves observable on device (per Task 2 checklist).
- All 12 acceptance criteria from UI-SPEC §"Page 2 — Pain Cards / Acceptance criteria" pass.
- Reduce Motion mode shows fade-only transitions; haptics still fire.
- Plan 04 (Plan Reveal) downstream consumer of `receiptCount` not regressed.
</verification>

<success_criteria>
- All 6 D-01 fixes applied with verbatim UI-SPEC values
- Device build + install succeed on `00008130-000A214E11E2001C`
- Human-verified on iPhone (light + dark, Reduce Motion on + off)
- `onContinue(Int)` callback signature preserved
- No "QUESTION X/Y" / "Yep" / "Nah" / cream-paper chrome remains
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-final/01-01-SUMMARY.md` per `templates/summary.md`.
</output>
